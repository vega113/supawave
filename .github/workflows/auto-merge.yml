name: Auto-Merge

on:
  pull_request:
    branches: [ main ]
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

jobs:
  enable-auto-merge:
    name: Enable Auto-Merge
    if: ${{ !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: write
    steps:
      - name: Enable squash auto-merge
        run: |
          if ! gh pr merge --auto --squash "$PR_URL"; then
            echo "::warning::Failed to enable auto-merge for $PR_URL. Check repository settings, token permissions, or merge conflicts."
          fi
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
