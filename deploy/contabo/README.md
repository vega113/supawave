# Legacy Provider Overlay

The canonical deployment assets now live under `deploy/caddy/`.

This directory is retained only as a legacy/provider-specific compatibility
surface while existing documentation and automation references are being
migrated. New deployment work should use the provider-neutral assets in
`deploy/caddy/` and the generic Linux host documentation.
